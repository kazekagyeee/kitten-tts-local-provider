services:
  silero-tts:
    build: .
    container_name: silero-tts
    ports:
      # PORT from .env is the host port; the app listens on 8000 in the container.
      - "${PORT:-8000}:8000"
    environment:
      - PORT=8000
      - HOST=0.0.0.0
      - SAMPLE_RATE=${SAMPLE_RATE}
      - DEFAULT_VOICE=${DEFAULT_VOICE}
      - DEFAULT_LANGUAGE=${DEFAULT_LANGUAGE}
      - MODEL_VERSION=${MODEL_VERSION}
    volumes:
      - ./models:/root/.cache/torch # Cache models here
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
